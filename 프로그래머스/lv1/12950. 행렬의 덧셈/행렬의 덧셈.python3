# LEVEL 1 행렬의 덧셈
# numpy를 사용하여 풀어보기
# 입력받는 arr1, arr2는 array가 요소로 들어있는 array -> 2차원 배열

import numpy as np

def solution(arr1, arr2):
    answer = [[]]

    # numpy를 사용하여 np.array로 만들기
    np_arr1 = np.array(arr1)
    np_arr2 = np.array(arr2)

    answer = np_arr1 + np_arr2 # np화 한 array를 더해주기
    
    return answer.tolist()

# tolist() : array를 list로 변환하는 numpy의 함수